<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Order Confirmation</title>
  <style>
    body { font-family: Arial, sans-serif; color: #333; background-color: #f9f9f9; padding: 20px; }
    .container { max-width: 700px; margin: auto; padding: 20px; background-color: #fff; border-radius: 8px; box-shadow: 0 2px 8px rgba(0,0,0,0.1); }
    h1 { color: #4CAF50; }
    .shipping, .products, .total-section { margin-top: 20px; }
    .product-card { display: flex; border: 1px solid #eee; border-radius: 8px; padding: 10px; margin-bottom: 10px; background: #fafafa; }
    .product-img { width: 100px; height: 100px; object-fit: cover; border-radius: 4px; margin-right: 15px; }
    .product-info { flex: 1; }
    .product-title { font-weight: bold; font-size: 1em; margin-bottom: 5px; }
    .product-desc { font-size: 0.9em; color: #555; margin-bottom: 5px; }
    .product-qty-price { display: flex; justify-content: space-between; font-weight: bold; }
    .total { text-align: right; font-size: 1.2em; font-weight: bold; margin-top: 10px; }
  </style>
</head>
<body>
  <div class="container">
    <h1>Order Confirmed!</h1>
    <p>Hi <%= user.firstName %>,</p>
    <p>Thank you for shopping with ShiCraft. Your order <strong>#<%= order._id %></strong> has been confirmed.</p>

    <div class="shipping">
      <h2>Shipping Address:</h2>
      <p>
        <%= shipping.firstName %> <%= shipping.lastName %><br>
        <%= shipping.houseNo %>, <%= shipping.street %><br>
        <%= shipping.city %>, <%= shipping.state %>, <%= shipping.country %> - <%= shipping.pincode %><br>
        Phone: <%= shipping.phone %>
      </p>
    </div>

    <div class="products">
      <h2>Order Details:</h2>
      <% order.items.forEach(item => { %>
        <div class="product-card">
          <img class="product-img" src="<%= item.productId.images[0] %>" alt="<%= item.productId.title %>">
          <div class="product-info">
            <div class="product-title"><%= item.productId.title %></div>
            <% if(item.productId.description){ %>
              <div class="product-desc"><%= item.productId.description %></div>
            <% } %>
            <div class="product-qty-price">
              <p>Qty: <%= item.quantity || 1 %></p>
              <p>RM<%= item.productId.price %></p>
            </div>
          </div>
        </div>
      <% }) %>
    </div>

    <p>We hope you enjoy your purchase!</p>
  </div>
</body>
</html>
